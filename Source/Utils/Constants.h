#pragma once

namespace Constants
{
    const int WINDOW_WIDTH = 1440;
    const int WINDOW_HEIGHT = 900;
    const int SIDEBAR_WIDTH = 200;

    namespace Colors
    {
        const juce::Colour background = juce::Colour(0xFF2D2D2D);
        const juce::Colour primary = juce::Colour(0xFF007AFF);
        const juce::Colour text = juce::Colour(0xFFFFFFFF);
    }
}